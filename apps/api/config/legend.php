<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Admin password
    |--------------------------------------------------------------------------
    |
    | The single read-only admin account uses the username "admin". The
    | password is compared with this environment value and is never stored
    | in the database or sent to the frontend.
    |
    */

    'admin_password' => env('ADMIN_PASSWORD'),

];
